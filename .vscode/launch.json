{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}"
      ],
      "env": {
        "WAITSTATE_PUBLISHER_ID": "pub-dev-test",
        "WAITSTATE_API_BASE_URL": "http://127.0.0.1:8787"
      }
    }
  ]
}
